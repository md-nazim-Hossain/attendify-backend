export enum ENUM_EMPLOYEE_ROLE {
  EMPLOYEE = 'employee',
  ADMIN = 'admin',
}

export enum ENUM_EMPLOYEE_STATUS {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  PENDING = 'pending',
  REJECTED = 'rejected',
}

export enum ENUM_EMPLOYEE_GENDER {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}
