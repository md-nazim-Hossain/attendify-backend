export enum ENUM_PROJECT_STATUS {
  IN_PROGRESS = 'in-progress',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  ON_HOLD = 'on-hold',
  REVIEW = 'review',
}

export enum ENUM_TEAM_STATUS {
  IN_PROGRESS = 'in-progress',
  COMPLETED = 'completed',
  ON_HOLD = 'on-hold',
  REVIEW = 'review',
}
